name: Example
description: Example app for the story_video_player package.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  video_player: 
  cupertino_icons: ^1.0.2
  story_video_player:
    path: '../'

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true